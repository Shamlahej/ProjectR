<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="ProjectR.MainWindow"
        Width="800"
        Height="520"
        Title="ProjectR">

    <Grid RowDefinitions="Auto,*,Auto">
        <TabControl Name="TabControl" Grid.Row="0">

            <TabItem Header="🔑 Login" Name="LoginTab">
                <StackPanel Margin="12" Spacing="8">
                    <TextBox Name="LoginUsername" Watermark="username" Text="admin"/>
                    <TextBox Name="LoginPassword" Watermark="password" PasswordChar="●" Text="admin"/>
                    <Button Click="LoginButton_OnClick">Login</Button>
                </StackPanel>
            </TabItem>

            <TabItem Header="🦾 Robot" Name="RobotTab">
                <StackPanel Margin="12" Spacing="10">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock VerticalAlignment="Center" Text="Robot IP:"/>
                        <TextBox Name="IpAddress" Width="220" Text="172.20.254.207"/>
                        <Button Click="ConnectButton_OnClick">Connect</Button>
                        <Button Click="SendScriptButton_OnClick">Send robot.script</Button>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Click="SimOk_OnClick">+1 OK</Button>
                        <Button Click="SimReject_OnClick">+1 Reject</Button>
                        <Button Click="ShowCounter_OnClick">Show counter</Button>
                    </StackPanel>
                </StackPanel>
            </TabItem>

            <TabItem Header="👥 Users" Name="UsersTab">
                <StackPanel Margin="12" Spacing="8">
                    <TextBlock FontWeight="Bold" Text="Create User"/>
                    <TextBox Name="CreateUserUsername" Watermark="username"/>
                    <TextBox Name="CreateUserPassword" Watermark="password" PasswordChar="●"/>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock VerticalAlignment="Center" Text="Is admin"/>
                        <CheckBox Name="CreateUserIsAdmin"/>
                    </StackPanel>
                    <Button Click="CreateUserButton_OnClick">Register</Button>
                </StackPanel>
            </TabItem>

            <TabItem Header="🛢 Database" Name="DatabaseTab">
                <StackPanel Margin="12" Spacing="8">
                    <Button Click="RecreateDatabaseButton_OnClick">Recreate database</Button>
                    <Button Click="ShowCounter_OnClick">Show counter</Button>
                </StackPanel>
            </TabItem>

        </TabControl>

        <Border Grid.Row="1" Background="LightGray" BorderBrush="Black"
                BorderThickness="0,1,0,1" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Name="LogOutput" TextWrapping="Wrap" FontFamily="Consolas"/>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="2" ColumnDefinitions="*,Auto">
            <Button Grid.Column="0" Click="ClearLogButton_OnClick">Clear</Button>
            <Button Grid.Column="1" Name="LogoutButton" IsVisible="False" Click="LogoutButton_OnClick" Margin="8,0,0,0">
                Logout
            </Button>
        </Grid>
    </Grid>
</Window>
